import fs from 'node:fs/promises';
async function walk(dir){for(const e of await fs.readdir(dir,{withFileTypes:true})){const p=dir+'/'+e.name;if(e.isDirectory())await walk(p);else if(p.endsWith('.html')){let s=await fs.readFile(p,'utf8');s=s.replaceAll('?v=8','?v=9');if(p.includes('/privacy/'))s=s.replace('<section><h2>Retention</h2>','<section><h2>Portfolio and owner studio</h2><p>The studio is restricted to the site owner using the hosting platform’s signed-in identity. The owner uploads photos, videos, descriptions and optional captions. Drafts and archived pieces are restricted to the owner; published pieces are visible to the site’s permitted audience. Media is stored with the hosting provider and served directly, without third-party video embeds. Portfolio files remain until removed by the operator; archiving hides a piece but does not delete it. Contact Spencer about a depicted person’s privacy or permissions.</p></section><section><h2>Retention</h2>');await fs.writeFile(p,s);}}}await walk('web');
