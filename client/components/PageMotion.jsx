import {useLayoutEffect} from 'react';import {gsap} from 'gsap';import {ScrollTrigger} from 'gsap/ScrollTrigger';gsap.registerPlugin(ScrollTrigger);
export default function PageMotion({paused,root,watch}){useLayoutEffect(()=>{if(paused)return;const ctx=gsap.context(()=>{gsap.utils.toArray('[data-arrive]').forEach(el=>gsap.from(el,{y:42,opacity:0,duration:.85,ease:'power3.out',scrollTrigger:{trigger:el,start:'top 92%',once:true}}));gsap.utils.toArray('.page-hero h1 .word-wrap>span').forEach((el,i)=>gsap.from(el,{yPercent:115,duration:1,delay:i*.1,ease:'power3.out'}));gsap.utils.toArray('[data-drift]').forEach(el=>gsap.fromTo(el,{yPercent:7},{yPercent:-7,ease:'none',scrollTrigger:{trigger:el.parentElement,start:'top bottom',end:'bottom top',scrub:true}}));},root);ScrollTrigger.refresh();return()=>ctx.revert();},[paused,watch]);return null;}
